# Flask Core
Flask==3.0.0
Flask-CORS==4.0.0
Flask-JWT-Extended==4.6.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
python-dotenv==1.0.0

# Database
# psycopg2-binary is optional - only needed for direct PostgreSQL connection
# We use Supabase REST API, so this is not required
# psycopg2-binary>=2.9.9  # Uncomment if you need direct PostgreSQL access
SQLAlchemy==2.0.23
alembic==1.13.1

# Supabase Client (for REST API)
supabase==2.3.4
httpx[http2]>=0.25.2,<0.28  # Compatible with supabase 2.3.4 (proxy parameter fix)
httpcore>=1.0.9  # Required for httpx

# Authentication & Security
bcrypt==4.1.2
pyotp==2.9.0
cryptography==41.0.7

# QR Code Generation
qrcode[pil]==7.4.2
Pillow>=10.2.0  # Updated for Python 3.14 compatibility

# Email & SMS
sendgrid==6.11.0
twilio==8.10.0

# Validation
marshmallow==3.20.1
marshmallow-sqlalchemy==0.29.0
email-validator==2.1.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3
uuid==1.30

# API Documentation
flask-swagger-ui==4.11.1
flasgger==0.9.7.1

# Testing
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0

# Production
gunicorn==21.2.0
gevent==23.9.1

